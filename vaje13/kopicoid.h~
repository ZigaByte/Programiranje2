#ifndef _KOPICOID_ 
#define _KOPICOID_

typedef struct kopica{
	int value;
	struct kopica* left;
	struct kopica* right;
} kopica;

void izpisi(kopica* k);
kopica* dodaj(kopica* a, kopica* b);
kopica* odstrani(kopica* a);

#endif
